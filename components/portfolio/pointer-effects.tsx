"use client";
import { useEffect } from 'react';
export function PointerEffects(){useEffect(()=>{const media=matchMedia('(pointer:fine) and (prefers-reduced-motion:no-preference)');const move=(event:PointerEvent)=>{if(!media.matches)return;const hero=document.querySelector<HTMLElement>('.hero');if(hero){hero.style.setProperty('--pointer-x',event.clientX+'px');hero.style.setProperty('--pointer-y',event.clientY+'px');}const target=(event.target as Element).closest<HTMLElement>('.button,.slider-controls button');if(target){const box=target.getBoundingClientRect();target.style.translate=`${(event.clientX-box.left-box.width/2)*.12}px ${(event.clientY-box.top-box.height/2)*.12}px`;}};const leave=(event:PointerEvent)=>{const target=(event.target as Element).closest<HTMLElement>('.button,.slider-controls button');if(target)target.style.translate='0px 0px';};window.addEventListener('pointermove',move,{passive:true});document.addEventListener('pointerout',leave);return()=>{window.removeEventListener('pointermove',move);document.removeEventListener('pointerout',leave);};},[]);return null;}
