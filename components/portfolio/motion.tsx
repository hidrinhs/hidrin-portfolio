"use client";
import { useEffect } from 'react';
export function Motion(){useEffect(()=>{let cleanup=()=>{};let disposed=false;(async()=>{const {gsap}=await import('gsap');const {ScrollTrigger}=await import('gsap/ScrollTrigger');const {default:Lenis}=await import('lenis');if(disposed)return;gsap.registerPlugin(ScrollTrigger);const mm=gsap.matchMedia();mm.add('(prefers-reduced-motion: no-preference)',()=>{const lenis=new Lenis({anchors:true});const tick=(time:number)=>lenis.raf(time*1000);lenis.on('scroll',ScrollTrigger.update);gsap.ticker.add(tick);const context=gsap.context(()=>{gsap.utils.toArray<HTMLElement>('[data-reveal]').forEach(el=>gsap.from(el,{y:55,opacity:0,duration:1,scrollTrigger:{trigger:el,start:'top 92%',once:true}}));gsap.from('.hero-title span',{y:90,opacity:0,duration:1.2,stagger:.12,ease:'power3.out'});});return()=>{context.revert();gsap.ticker.remove(tick);lenis.destroy();};});cleanup=()=>mm.revert();})();return()=>{disposed=true;cleanup();};},[]);return null;}
